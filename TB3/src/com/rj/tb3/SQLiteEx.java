package com.rj.tb3;

import android.app.Activity;

public class SQLiteEx extends Activity //implements OnClickListener
{
	/*private EditText name, scale;
	private Button up, vw;
	
	@Override
	protected void onCreate(Bundle savedInstanceState) 
	{
		setContentView(R.layout.sqlscale);
		super.onCreate(savedInstanceState);
		name = (EditText)findViewById(R.id.name);
		scale = (EditText)findViewById(R.id.scale);
		up = (Button)findViewById(R.id.up);
		vw = (Button)findViewById(R.id.vw);
		up.setOnClickListener(this);
		vw.setOnClickListener(this);
	}

	@Override
	public void onClick(View arg0) 
	{
		switch (arg0.getId())
		{
		case R.id.up:	break;
		
		case R.id.vw:	break;
				
		}
		
	}*/
	
}
